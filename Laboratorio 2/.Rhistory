library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter = list(support = 0.1, confidence = 0.3))
rules.sorted <- sort(rules, by = "support")
inspect(head(rules.sorted, 7))
View(rules)
View(rules.sorted)
# RESPUESTA
rules <- apriori(Groceries, parameter = list(support = 0.1, confidence = 0.3))
rules.sorted <- sort(rules, by = "support")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules <- apriori(Groceries, parameter = list(support = 0.1, confidence = 0.3))
rules_sorted <- sort(rules, by = "support")
inspect(head(rules_sorted, 7))
# RESPUESTA
rules <- apriori(Groceries, parameter = list(support = 0.1, confidence = 0.3))
rules.sorted <- sort(rules, by = "support")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules <- apriori(df, parameter = list(support = 0.1, confidence = 0.3))
# RESPUESTA
rules <- apriori(Groceries, parameter = list(support = 0.1, confidence = 0.3))
rules.sorted <- sort(rules, by = "support")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules <- apriori(Groceries, parameter = list(support = 0.1, confidence = 0.3))
rules.sorted <- sort(rules, by = "support")
inspect(head(rules.sorted, 7))
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter = list(support = 0.1, confidence = 0.3))
rules.sorted <- sort(rules, by = "support")
inspect(head(rules.sorted, 7))
```{r}
rules.sorted <- sort(rules, by = "support")
inspect(head(rules.sorted, 7))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
# RESPUESTA
rules <- apriori(Groceries, parameter = list(support = 0.1, confidence = 0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter = list(support = 0.1, confidence = 0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.2, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.2, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.2, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.2, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.2, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.2, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
gc()
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.2, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.1, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
rules2 <- apriori(Groceries, parameter = list(support = 0.02, confidence = 0.3))
rules2_sorted <- rule2[sort(rules2, by = "lift"), ]
# RESPUESTA
rules2 <- apriori(Groceries, parameter = list(support = 0.02, confidence = 0.3))
rules2.sorted <- sort(rules2, by = "lift")
inspect(head(rules2.sorted, 10))
# Lift estima la probabilidad condicional de que se compre un conjunto de productos Y
# dada la aparacion de itemset X (i.e. estima P(Y|X)).
# Por definicion de probabilidad, lift nos da una medida de la certeza de que se compre Y dado que se compra X
# que es precisamente lo que más le podría ayuda al supermercado (comercialmente)
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.1))
plot(rules)
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
hist(rules.sorted@quality$support, main = "Histograma de Soporte", xlab = "Soporte")
# RESPUESTA
rules2 <- apriori(Groceries, parameter = list(support = 0.02, confidence = 0.3))
rules2.sorted <- sort(rules2, by = "lift")
inspect(head(rules2.sorted, 10))
# RESPUESTA
rules2_dec <- rules2
rules2_dec <- sort(rules2_dec, by = "lift", decreasing = FALSE)
inspect(head(rules2_dec, 1))
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.1))
plot(rules)
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
hist(rules.sorted@quality$support, main = "Histograma de Soporte", xlab = "Soporte")
# RESPUESTA
rules2 <- apriori(Groceries, parameter = list(support = 0.02, confidence = 0.3))
rules2.sorted <- sort(rules2, by = "lift")
inspect(head(rules2.sorted, 10))
# RESPUESTA
rules2_dec <- rules2
rules2_dec <- sort(rules2_dec, by = "lift", decreasing = FALSE)
inspect(head(rules2_dec, 1))
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.1))
plot(rules)
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
rules.sorted <- sort(rules, by="lift")
inspect(head(rules.sorted, 7))
# RESPUESTA
hist(rules.sorted@quality$support, main = "Histograma de Soporte", xlab = "Soporte")
# RESPUESTA
rules2 <- apriori(Groceries, parameter = list(support = 0.02, confidence = 0.3))
rules2.sorted <- sort(rules2, by = "lift")
inspect(head(rules2.sorted, 10))
# RESPUESTA
rules2_dec <- rules2
rules2_dec <- sort(rules2_dec, by = "lift", decreasing = FALSE)
inspect(head(rules2_dec, 1))
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.1))
plot(rules)
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
rules.sorted <- sort(rules, by="support")
inspect(head(rules.sorted, 7))
# RESPUESTA
hist(rules.sorted@quality$support, main = "Histograma de Soporte", xlab = "Soporte")
# RESPUESTA
rules2 <- apriori(Groceries, parameter = list(support = 0.02, confidence = 0.3))
rules2.sorted <- sort(rules2, by = "lift")
inspect(head(rules2.sorted, 10))
# RESPUESTA
rules2_dec <- rules2
rules2_dec <- sort(rules2_dec, by = "lift", decreasing = FALSE)
inspect(head(rules2_dec, 1))
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.1))
plot(rules)
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
rules.sorted <- sort(rules, by="support") # La ocurrencia esta ligada al support
inspect(head(rules.sorted, 7))
# RESPUESTA
hist(rules.sorted@quality$support, main = "Histograma de Soporte", xlab = "Soporte")
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
rules.sorted <- sort(rules, by="support") # La ocurrencia esta ligada al support
inspect(head(rules.sorted, 7))
# RESPUESTA
hist(rules.sorted@quality$support, main = "Histograma de Soporte", xlab = "Soporte")
# RESPUESTA
rules2 <- apriori(Groceries, parameter = list(support = 0.02, confidence = 0.3))
rules2.sorted <- sort(rules2, by = "lift")
inspect(head(rules2.sorted, 10))
# RESPUESTA
rules2_dec <- rules2
rules2_dec <- sort(rules2_dec, by = "lift", decreasing = FALSE)
inspect(head(rules2_dec, 1))
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.1))
plot(rules)
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
rules.sorted <- sort(rules, by="support") # La ocurrencia esta ligada al support
inspect(head(rules.sorted, 7))
# RESPUESTA
hist(rules.sorted@quality$support, main = "Histograma de Soporte", xlab = "Soporte")
# RESPUESTA
rules2 <- apriori(Groceries, parameter = list(support = 0.02, confidence = 0.3))
rules2.sorted <- sort(rules2, by = "lift")
inspect(head(rules2.sorted, 10))
# RESPUESTA
rules2_dec <- rules2
rules2_dec <- sort(rules2_dec, by = "lift", decreasing = FALSE)
inspect(head(rules2_dec, 1))
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.1))
plot(rules)
library("arules")
library("arulesViz")
data(Groceries)
inspect(head(Groceries))
# RESPUESTA
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.3))
rules.sorted <- sort(rules, by="support") # La ocurrencia esta ligada al support
inspect(head(rules.sorted, 7))
# RESPUESTA
hist(rules.sorted@quality$support, main = "Histograma de Soporte", xlab = "Soporte")
# RESPUESTA
rules2 <- apriori(Groceries, parameter = list(support = 0.02, confidence = 0.3))
rules2.sorted <- sort(rules2, by = "lift")
inspect(head(rules2.sorted, 10))
# RESPUESTA
rules2_dec <- rules2
rules2_dec <- sort(rules2_dec, by = "lift", decreasing = FALSE)
inspect(head(rules2_dec, 1))
rules <- apriori(Groceries, parameter=list(support=0.01, confidence=0.1))
plot(rules)
